using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class Character : MonoBehaviour {


/* 
	protected Health theHealthClassThing;
	protected int moveSpeed;
	protected Dictionary<KeyCode, Func<Boolean>> abilities;
	protected Collider2D hitbox;

	protected void detectCollision() {
		//if other.gameobj is attack
		//then takeDakamge()
	}

	public Character(int moveSpeed, Collider2D hitbox) {
		this.hitbox = hitbox;
		this.moveSpeed=moveSpeed;
		theHealthClassThing = new Health();
	}

	// Use this for initializations
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
	void KeyPressDown(KeyCode given) {
		//if abilities.ContainsKey(given):
		//	abilities[given].run()
	}
*/
}
